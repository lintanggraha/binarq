import 'package:flutter/material.dart';

class AppColors {
  // Primary (Cyan) - Menyegarkan, menenangkan
  static const Color primary = Color(0xFF00C4B5);
  static const Color primaryDark = Color(0xFF009B8F);

  // Secondary (Orange) - Enerjik, memancing interaksi
  static const Color secondary = Color(0xFFFF8C42);

  // Accent (Yellow) - Koin/Bintang XP
  static const Color accent = Color(0xFFFFD166);

  // Feedback Colors
  static const Color error = Color(0xFFEF476F); // Soft Red
  static const Color success = Color(0xFF06D6A0); // Positif Green

  // Backgrounds
  static const Color background = Color(0xFFF8FBFF); // Soft Blue
  static const Color surface = Colors.white;

  // Text Colors
  static const Color textDark = Color(0xFF1E293B);
  static const Color textLight = Color(0xFF64748B);
}
